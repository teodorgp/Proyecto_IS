//curso.cc

#include "curso.h"

Curso::Curso(string nombre, string descripcion, string fecha_inicio, string fecha_fin)
{
  setNombre(nombre);
  setDescripcion(descripcion);
  setFecha_inicio(fecha_inicio);
  setFecha_fin(fecha_fin);
}